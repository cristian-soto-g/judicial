# Dependencias de ejecución. Todas se instalan y se ejecutan localmente:
# ninguna realiza llamadas de red durante el procesamiento de documentos.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
pydantic>=2.8,<3.0

# Lectura de documentos (sin OCR)
python-docx>=1.1,<2.0
pdfplumber>=0.11,<0.13

# Exportación
reportlab>=4.0

# Resolución de identidades
rapidfuzz>=3.9,<4.0
networkx>=3.2,<4.0

# Reversibilidad: cifrado del mapa de correspondencias (AES-GCM + scrypt)
cryptography>=41.0
